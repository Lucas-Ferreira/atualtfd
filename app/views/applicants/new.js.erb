$("#exampleModall").find(".modal-content").html("<%= j render(:partial => 'applicants/formmodal', :locals => {:travel => @travel, :patient => @patient, :state => @state}) %>");
setTimeout(() => {
  document.location.reload();
}, 500);
